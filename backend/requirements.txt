fastapi==0.109.0
uvicorn[standard]==0.27.0
websockets==12.0
python-multipart==0.0.6
pydantic==2.5.0

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Utilities
python-dotenv==1.0.0
redis==5.0.1

# LLM Providers (use any one or all)
openai>=1.12.0          # Used for OpenAI AND Ollama (compatible API)
anthropic>=0.20.0       # Used for Anthropic Claude
litellm>=1.35.0         # Optional: 100+ providers (groq, mistral, bedrock, etc.)

# Async HTTP (for http_tool)
httpx>=0.26.0

# Web search (free, no API key)
duckduckgo-search>=4.4.0
